// Code auto-generated by dqlx DO NOT EDIT!!

package {{ .PackageName }}

import (
{{ range $i, $element := .Imports -}}
     "{{ $i }}"
{{- end }}
)


{{range $element := .Types}}
    var {{ $element.Name }}Fields = []string{
        {{- range $i, $field := .Fields }}
            {{- if not $field.IsEdge }} "{{ $field.JsonName }}", {{ end -}}
        {{- end}}
    }

    {{- range $i, $field := .Fields }}
        {{- if $field.IsEdge }}
            var {{ $element.Name }}To{{ $field.Name }}Edge = "{{ $field.JsonName }}"
        {{ end -}}
    {{- end}}

    var {{ $element.Name }}Type = "{{ $element.Name }}"

    type {{ $element.Name }} struct {
    {{- range $i, $field := .Fields}}
        {{ $field.Name }} {{ $field.GoType }} {{if $field.JsonName}} `json:"{{ $field.JsonName }},omitempty"`{{- end}}
    {{- end}}
}
{{end}}